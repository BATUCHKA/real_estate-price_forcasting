# Үл хөдлөх хөрөнгийн үнийн таамаглал

НУМ-ийн "Магадлал" хичээлийн багийн төслийн ажил. unegui.mn-ээс цуглуулсан орон сууцны зарын өгөгдөл дээр машин сургалтын регрессийн загвараар үнэ таамаглах.

## Сэдэв ба зорилго

**Асуудал**: Орон сууцны үнэ нь талбай, байршил, барилгын нас, давхарлалт, тоноглол зэрэг олон хүчин зүйлээс хамаардаг. Эдгээр хүчин зүйлийг ашиглан үнийг хэр нарийвчилж урьдчилан таамаглаж болох вэ?

**Зорилго**:
1. Бодит зах зээлийн өгөгдөл цуглуулах (web scraping)
2. Хувьсагчдын хамаарлыг статистик аргаар судлах
3. Регрессийн хэд хэдэн загвар харьцуулах (Шугаман, Lasso, Шийдвэрийн мод, Random Forest)
4. Үнэд хамгийн их нөлөөлөх хүчин зүйлсийг тогтоох

## Багийн гишүүд

| Гишүүн | Үүрэг |
|---|---|
| _<нэр>_ | Өгөгдөл цуглуулалт, scraper |
| _<нэр>_ | Цэвэрлэлт, EDA |
| _<нэр>_ | Шугаман загварууд (LR, Lasso, Ridge) |
| _<нэр>_ | Модны загварууд (Decision Tree, Random Forest) |
| _<нэр>_ | Тайлан, үзүүлэн, харьцуулалт |

## Төслийн бүтэц

```
magadlal/
├── README.md
├── requirements.txt
├── scrape_unegui.py            # 1. Өгөгдөл цуглуулагч
├── data/
│   ├── test.csv                # Туршилтын түүвэр (60 зар)
│   ├── listings.csv            # Бүрэн өгөгдөл (scrape-ийн дараа)
│   └── listings_clean.csv      # Цэвэрлэсэн өгөгдөл
├── notebooks/
│   ├── 02_clean.ipynb          # 2. Цэвэрлэлт
│   ├── 03_eda.ipynb            # 3. Судлах өгөгдлийн анализ
│   └── 04_models.ipynb         # 4. Загварчлал ба харьцуулалт
├── report/
│   ├── report.qmd              # Quarto тайлангийн эх файл
│   └── report.pdf              # Эцсийн тайлан
└── presentation/
    └── slides.pdf              # Үзүүлэн слайд
```

## Суулгах

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Ажиллуулах дараалал

### 1. Өгөгдөл цуглуулах

```bash
# Туршилт (~2 минут, 60 зар)
python scrape_unegui.py --pages 1 --out data/test.csv

# Бүрэн (~2 цаг, ~3000 зар) — шөнө ажиллуулах нь зүйтэй
python scrape_unegui.py --pages 50 --out data/listings.csv
```

Скрипт нь өмнө татсан зарыг алгасдаг тул дахин ажиллуулахад аюулгүй.

### 2. Цэвэрлэлт

```bash
jupyter notebook notebooks/02_clean.ipynb
```

`data/listings.csv` → `data/listings_clean.csv`

Хийгддэг үйлдлүүд:
- Давхардсан зар арилгах
- Үнэ, талбай, оны outlier хасах
- Категорийн хувьсагч кодлох (`has_balcony`, `has_garage`, `has_elevator`)
- Шинэ feature: `price_per_m2`, `building_age`, `log_price`, `floor_ratio`

### 3. EDA

```bash
jupyter notebook notebooks/03_eda.ipynb
```

Үнийн тархалт, корреляцийн матриц, дүүрэг тус бүрийн боксплот, гипотез тогтоох.

### 4. Загварчлал

```bash
jupyter notebook notebooks/04_models.ipynb
```

Харьцуулах загварууд:

| Загвар | Зорилго |
|---|---|
| Шугаман регресс | Baseline, коэффициентийн тайлбар |
| Lasso / Ridge | Регулярчилал, чухал хувьсагч сонгох |
| Шийдвэрийн мод | Шугаман бус хамаарал, тайлбарлахад хялбар |
| Random Forest | Хамгийн өндөр нарийвчлал, feature importance |

**Үнэлгээний хэмжүүр**: RMSE, MAE, R² (тест дээр)

### 5. Шинэ айлын үнэ таамаглах

```bash
# Жишээ таамаглал (5 өөр төрлийн айл)
python predict.py --examples

# Тодорхой айл
python predict.py --area 80 --rooms 3 --district Хан-Уул --age 5 --balcony --elevator

# Бүх параметр
python predict.py \
    --area 95 --rooms 3 --floor 7 --total-floors 12 \
    --age 3 --district Сүхбаатар \
    --balcony --garage --elevator --finished
```

Эхний удаа ажиллуулахад загварыг сургаж `models/rf_model.joblib`-д хадгална.
Дараагийн удаад хадгалсан загварыг шууд ачаалдаг → секундын дотор хариу өгнө.

## Өгөгдлийн тухай

**Эх сурвалж**: [unegui.mn](https://www.unegui.mn/l-hdlh/l-hdlh-zarna/oron-suuts-zarna/) — Монгол улсын хамгийн том зар сурталчилгааны платформ.

**Цуглуулсан хувьсагч (22)**:

| Багана | Тайлбар |
|---|---|
| `price_mnt` | Үнэ (₮) — таамаглах гол хувьсагч |
| `area_m2` | Талбай (м²) |
| `rooms` | Өрөөний тоо |
| `floor` / `total_floors` | Хэдэн давхарт / нийт давхар |
| `year_built` | Ашиглалтад орсон он |
| `district` / `khoroo` | Дүүрэг / хороо |
| `balcony` / `garage` / `elevator` | Тагт, гараж, цахилгаан шат |
| `window_type` / `door_type` / `floor_material` | Барилгын материал |
| `construction_status` | Барилгын явц |
| `payment_terms` | Төлбөрийн нөхцөл |

**Ёс зүй**:
- robots.txt-д тус хэсгийн scraping хориглогдоогүй
- Хүсэлт хооронд 1.5 секунд хүлээж серверт ачаалал өгөхгүй
- Хувийн мэдээлэл (утас, зарагчийн нэр) хадгалахгүй

## Үр дүн

**Өгөгдөл**: 2965 цэвэр зар (3000-аас 33 outlier хасав). Train/test 80/20 хуваалт.

| Загвар | RMSE (сая ₮) | MAE (сая ₮) | R² |
|---|---|---|---|
| Шугаман регресс | 314.2 | 142.4 | 0.833 |
| Lasso (α=0.01) | 304.9 | 142.0 | 0.823 |
| Ridge (α=1.0) | 313.6 | 142.3 | 0.833 |
| Шийдвэрийн мод | 209.0 | 99.5 | 0.883 |
| **Random Forest** | **184.2** | **85.0** | **0.905** |
| **Random Forest (tuned)** | **173.1** | — | **0.911** |

**Хамгийн сайн загвар**: Random Forest (n=400, max_depth=None, min_samples_leaf=1) — тестийн **R² 0.91**, RMSE **173 сая ₮**.

**Гол олигууд**:
- Шугаман бус хамаарал чухал — мод-суурьт загварууд шугаман загвараас RMSE-ийг ~45%-аар бууруулсан
- Талбай (м²) болон дүүрэг хамгийн чухал feature
- Шинэ барилга (`building_age` бага) өндөр үнэтэй

## Ашигласан хэрэгсэл

- Python 3.13
- pandas, numpy — өгөгдөл боловсруулалт
- scikit-learn — машин сургалтын загварууд
- matplotlib, seaborn — графикууд
- cloudscraper, beautifulsoup4 — web scraping
- Quarto — тайлан

## Лиценз ба хэрэглээ

Энэхүү төсөл нь зөвхөн академик зорилгоор хийгдсэн. Цуглуулсан өгөгдөл нь бодит түүвэр бөгөөд арилжааны зорилгоор ашиглах боломжгүй.
